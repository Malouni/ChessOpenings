<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".italian_practice"></activity>
        <activity android:name=".carokan_opening" />
        <activity android:name=".indian_opening" />
        <activity android:name=".queens_opening" />
        <activity android:name=".sicilian_opening" />
        <activity android:name=".italian_opening" />
        <activity android:name=".Practice_Activity" />
        <activity android:name=".opening_main_activity" />
        <activity android:name=".About_us" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>